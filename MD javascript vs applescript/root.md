```javascript
// root :: Tree a -> a
const root = tree =>
    // The value attached to a tree node.
    tree.root;
```


```applescript
-- root :: Tree a -> a
on root(oTree)
    root of oTree
end root
```