```javascript
// identity :: a -> a
const identity = x =>
    // The identity function.
    x;
```


```applescript
-- identity :: a -> a
on identity(x)
    -- The argument unchanged.
    x
end identity
```