```javascript
// drawTree :: Tree String -> String
const drawTree = tree =>
    unlines(draw(tree));
```


```applescript
-- drawTree :: Tree String -> String
on drawTree(tree)
    unlines(draw(tree))
end drawTree
```