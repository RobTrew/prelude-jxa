```applescript
-- Abbreviation for quick testing
```

```applescript
-- eft :: Enum a => a -> a -> [a]
on eft(m, n)
    enumFromTo(m, n)
end eft
```

```js
// Abbreviation for quick testing
```

```js
// eft :: Enum a => a -> a -> [a]
const eft = (m, n) => enumFromTo(m, n);
```