```applescript
-- stripStart :: String -> Stringon stripStart(s)	dropWhile(my isSpace, s)end stripStart
```

```js
// stripStart :: String -> String
const stripStart = s => dropWhile(isSpace, s);
```