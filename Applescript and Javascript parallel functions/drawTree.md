```applescript
-- drawTree :: Tree String -> Stringon drawTree(tree)	unlines(draw(tree))end drawTree
```

```js
// drawTree :: Tree String -> String
const drawTree = tree =>
    unlines(draw(tree));
```