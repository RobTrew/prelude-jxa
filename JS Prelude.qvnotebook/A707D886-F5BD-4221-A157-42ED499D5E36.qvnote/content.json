{
  "title": "dropWhile",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// dropWhile :: (a -> Bool) -> [a] -> [a]\n// dropWhile :: (Char -> Bool) -> String -> String\nconst dropWhile = p =>\n    xs => {\n        const n = xs.length;\n        return xs.slice(\n            0 < n ? until(\n                i => n === i || !p(xs[i])\n            )(i => 1 + i)(0) : 0\n        );\n    };"
    }
  ]
}