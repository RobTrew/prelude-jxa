{
  "title": "takeCycle",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// First n members of an infinite cycle of xs"
    },
    {
      "type": "code",
      "language": "javascript",
      "data": "// takeCycle :: Int -> [a] -> [a]\nconst takeCycle = (n, xs) => {\n    const lng = xs.length;\n    return (\n            n <= xs ? (\n                xs\n            ) : concat(\n                replicate(\n                    Math.ceil(n / lng),\n                    xs\n                )\n            )\n        )\n        .slice(0, n)\n};"
    }
  ]
}