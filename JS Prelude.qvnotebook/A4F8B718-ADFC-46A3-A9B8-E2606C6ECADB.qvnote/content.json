{
  "title": "minimum",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// minimum :: Ord a => [a] -> a\nconst minimum = xs =>\n    xs.length > 0 ? (\n        foldl1((a, x) => x < a ? x : a, xs)\n    ) : undefined;"
    }
  ]
}