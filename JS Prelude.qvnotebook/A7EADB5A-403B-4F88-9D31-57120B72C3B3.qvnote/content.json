{
  "title": "minimumMay",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// minimumMay :: [a] -> Maybe a\nconst minimumMay = xs =>\n    xs.length > 0 ? (\n        Just(xs.slice(1)\n            .reduce((a, x) => x < a ? x : a, xs[0])\n        )\n    ) : Nothing();"
    }
  ]
}