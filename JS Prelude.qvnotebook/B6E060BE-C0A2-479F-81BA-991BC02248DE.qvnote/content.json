{
  "title": "last",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// last :: [a] -> a\nconst last = xs => (\n    // The last item of a list.\n    ys => 0 < ys.length ? (\n        ys.slice(-1)[0]\n    ) : undefined\n)(list(xs));"
    }
  ]
}