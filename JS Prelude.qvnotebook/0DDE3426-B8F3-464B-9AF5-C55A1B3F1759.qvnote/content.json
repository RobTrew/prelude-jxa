{
  "title": "splitAt",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// splitAt :: Int -> [a] -> ([a], [a])\nconst splitAt = n =>\n    xs => Tuple(xs.slice(0, n))(\n        xs.slice(n)\n    );"
    }
  ]
}