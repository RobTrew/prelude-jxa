{
  "title": "showOutline",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// showOutline :: Tree String -> String\nconst showOutline = tree => {\n    const go = indent => x =>\n        unlines(\n            [indent + x.root]\n            .concat(x.nest.flatMap(go('    ' + indent)))\n        );\n    return go('')(tree);\n};"
    }
  ]
}