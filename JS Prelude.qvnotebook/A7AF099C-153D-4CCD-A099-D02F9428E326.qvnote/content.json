{
  "title": "toSentence",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// toSentence :: String -> String\nconst toSentence = s =>\n    // Sentence case - initial string capitalized \n    // and rest lowercase.\n    (0 < s.length) ? (\n        s[0].toUpperCase() + s.slice(1)\n        .toLowerCase()\n    ) : s;"
    }
  ]
}