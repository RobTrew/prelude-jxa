{
  "title": "fType",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// fType :: (a -> f b) -> f\nconst fType = g => {\n    const s = g.toString();\n    return s.includes('Left') ? (\n        Right\n    ) : s.includes('Nothing') ? (\n        Just\n    ) : s.includes('Node') ? (\n        flip(Node)([])\n    ) : x => [x];\n};"
    }
  ]
}