{
  "title": "showTuple",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// showTuple :: Tuple -> String\nconst showTuple = tpl =>\n    '(' + enumFromTo(0)(tpl.length - 1)\n    .map(x => unQuoted(show(tpl[x])))\n    .join(',') + ')';"
    }
  ]
}