{
  "title": "concatMap",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// concatMap :: (a -> [b]) -> [a] -> [b]\nconst concatMap = (f, xs) =>\n    xs.reduce((a, x) => a.concat(f(x)), []);\n    \n// Briefer but slower:\n// concatMap :: (a -> [b]) -> [a] -> [b]\n// const concatMap = (f, xs) =>\n//     [].concat(...xs.map(f))"
    }
  ]
}