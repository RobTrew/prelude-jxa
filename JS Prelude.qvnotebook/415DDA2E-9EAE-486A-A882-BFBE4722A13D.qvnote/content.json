{
  "title": "initMay",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// initMay :: [a] -> Maybe [a]\nconst initMay = xs => (\n    0 < ys.length ? (\n        Just(ys.slice(0, -1))\n    ) : Nothing()\n)(list(xs));"
    }
  ]
}