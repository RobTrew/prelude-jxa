{
  "title": "takeFileName",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// takeFileName :: FilePath -> FilePath\nconst takeFileName = strPath =>\n    strPath !== '' ? (\n        strPath[strPath.length - 1] !== '/' ? (\n            strPath.split('/')\n            .slice(-1)[0]\n        ) : ''\n    ) : '';"
    }
  ]
}