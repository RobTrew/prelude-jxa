{
  "title": "fromEnum",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "// fromEnum :: Enum a => a -> Int\nconst fromEnum = x =>\n    typeof x !== 'string' ? (\n        x.constructor === Object ? (\n            x.value\n        ) : parseInt(Number(x))\n    ) : x.codePointAt(0);"
    }
  ]
}